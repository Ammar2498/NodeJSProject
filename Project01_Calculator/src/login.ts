// type userCredential = {
//   userName: string;
//   UserPassword: number;
// };

// let Usercredential: userCredential[] = [];

// export function UserRegisteration(userName: string, UserPassword: number) {
//   Usercredential.push({ userName, UserPassword });
// }

// export function userSignIn(userName: string, UserPassword: number) {
//   let foundUser = Usercredential.find(function (user) {
//     return user.userName === userName && UserPassword === user.UserPassword;
//   });
//   return foundUser !== undefined;
// }
